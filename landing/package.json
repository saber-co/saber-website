{
  "name": "saber-website-vercel-root",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npx -y pnpm@9.15.9 -C .. install --frozen-lockfile && npx -y pnpm@9.15.9 -C .. --filter @workspace/landing run build && rm -rf dist && mkdir -p dist && cp -R ../artifacts/landing/dist/public/. dist/"
  }
}
